# Agent 工作规则

使用 OpenSpec 管理规格、设计和持久任务计划，由 Codex 主代理统一负责执行、调度、集成与完成判定。子代理、技能和自动化按需使用，不形成第二套规划或审批流程。

## 工作目标与边界

- 开始前读取适用的项目规则和相关开发文档，检查 `git status`，明确本轮目标、范围、约束和可验证的完成条件；优先明确预期结果，不为简单任务编排冗长流程。
- 遵循现有架构、代码规范和工具链；保护用户及其他代理的已有改动，不做无关重构，不擅自回退或覆盖改动。
- 在已明确的需求和授权内推进至本轮完成条件满足；仅要求分析、设计或评审时不擅自实施，实施任务则不因计划或部分修改完成而提前结束。不为普通实现反复确认，仅在无法从仓库解决的关键歧义、新增授权或必须由用户完成的操作阻塞时请求介入；其他独立工作可继续执行。
- 以当前环境实际权限为准；仅在确有需要且环境允许时申请提权。持续执行、子代理和自动化均不扩大授权；破坏性操作须先确认目标、影响及授权范围。用户要求暂停或环境无法继续时，记录状态并如实交接。

## OpenSpec 规格与变更

- OpenSpec 是项目规格与持久任务计划的唯一管理体系。`openspec/specs/` 记录现行约定；当前 change 记录本次拟议变更。实施依据是相关现行规格加上已确认的变更，不把未批准提案视为现行要求。
- 改变已记录的行为规格、公共接口、数据契约、架构或安全边界，以及高风险迁移、多阶段跨模块工作，应创建或更新 change；需要长期维护的新能力同样纳入。优先更新适用的现有 change，避免为同一目标重复建案。
- 恢复既定行为的局部修复、文档或格式调整、不改变行为的简单重构，可直接实施，无需为了流程创建 change；仍须遵守相关现行规格。
- 使用现有 OpenSpec 配置和 schema，按其要求维护产物。由主代理统一管理当前 change；子代理按委派范围执行，不自行创建或切换 change。
- 有当前 change 时，由主代理统一维护其 `tasks.md`，记录任务编号、依赖、必要的并发安排、验收条件和进度；关键决策写入设计，行为变更写入规格，可引用已有要求。允许当前会话内的临时执行清单，但不得替代持久计划，也不另建需要同步维护的平行规格、进度账本或审批流程。
- 已确认的范围直接执行，不重复请求授权。范围、关键设计或依赖关系发生实质变化时，先由主代理确认影响并更新相关产物；超出已确认需求、接口契约或授权边界的调整，取得用户确认后再实施。使用 Goal 模式时同样沿用这些依据，不另立规格或任务计划。
- 代码与规格不一致时，先判断是实现缺陷、规格遗漏还是需求变化，再按相应路径处理；不得为迁就代码或通过检查而擅自修改规格。

## Skills、脚本与资料：按需使用与持续改进

- 按任务涉及的行为、接口和依赖读取相关代码、规格、当前 change 的必要产物及参考资料；信息不足时沿关联范围扩展，不在每次任务或修改前预加载整个仓库、全部规格、全部 change 产物或全部技能正文。
- 跨工作树执行前确认目标 Git 根并读取该树及目标目录适用的规则；不假定工作树会继承物理父目录规则，也不把当前会话已加载的规则当成所有工作树的加载证明。
- 根据技能名称和描述判断适用性，再读取相关 `SKILL.md` 及当前任务需要的引用、脚本或其他资源；不因主题相近就加载无关技能。多流程技能采用渐进式加载，入口只提供必要的选择指引，具体流程和资源按需读取。
- Skills 用于提供可复用的专项方法或现有 OpenSpec 工作流的操作指引，可以辅助调查、实现、测试、调试、审查和产物维护，但不替代 OpenSpec 的规格与持久任务计划，不改变主代理的统一调度责任，也不另建默认流程框架或重复已完成的规划、授权确认和仍然有效的验证。
- 确定性操作优先复用仓库已有脚本和命令。重复、易错且具有明确复用价值的操作，再在授权范围内沉淀为脚本或技能；使用前确认适用条件和副作用，修改后验证其行为。
- 创建或维护技能时，保持名称和描述简短并明确具体触发条件，避免宽泛触发、职责重叠和不必要的固定步骤。复杂技能将详细流程、参考资料和脚本放入按需读取的资源中，不把所有指导堆入入口 `SKILL.md`。
- 更新工具生成或外部导入的技能前，按 [`docs/development-environment.md`](docs/development-environment.md#agent-configuration-maintenance) 保留本地定制并审阅来源差异，不直接覆盖受维护版本。
- 仅将经过验证且具有持续复用价值的经验沉淀到对应技能、脚本或开发文档；项目级长期约束才写入 `AGENTS.md`。不把未经验证的单次经验上升为全局规则，也不通过修改规则、规格或验收条件掩盖失败。
- 周期性或无人值守自动化仅依据明确授权和实际配置运行，不因 `AGENTS.md`、技能或脚本中存在相关能力而自行创建。

## 任务调度与子代理

### 调度与执行

- 主代理统一负责任务拆分、依赖分析、委派、集成和最终验收，也可直接实施。独立探索、边界清晰的实现或专项审查能够明显改善效率、上下文隔离或结果质量时，主动使用子代理；简单、紧密耦合或协调成本更高的任务直接完成，不固定代理数量，不为并行而并行。
- 委派时提供目标、相关任务编号或文件、必要上下文、读写范围、依赖和完成标准。子代理在委派范围内自主选择调查、实现和验证方法，可读取必要的关联代码；全局规划、写入归属调整和进一步委派由主代理负责。
- 并行前明确依赖和写入归属。依赖未满足的实施任务不得提前执行；共享文件、接口或可变运行资源的修改须协调或串行。主代理可同时处理独立工作，避免重复实施和写入冲突。
- 主代理在已授权范围内持续推进调查、实现、必要验证及本次改动引入问题的修复，直到用户目标及适用验收条件满足，不因首次实现或单个子任务完成而停下等待确认。子代理完成委派目标及约定验证后返回，不自行扩展任务范围；只读调查或审查不自动获得修改权限。
- 授权范围内的实现细节与任务安排由主代理自主调整。确需超出已授权变更范围，改变已确认需求、接口契约或授权边界时，由主代理说明影响并请求用户决定；存在无法解决的阻塞时，说明具体原因。等待决定或解除阻塞期间，可继续推进不受影响的已授权工作。

### 委派前选择模型与推理强度

- 委派前按子任务的关键判断、不确定性、跨模块耦合、风险和验证难度，直接选择足够胜任的模型与必要的推理强度；满足能力要求后再考虑成本，不按项目规模、文件数量或读写属性机械分档，不采用低档试做、失败后逐级升级的方式。
- 模型能力与推理强度分别判断：先按综合能力要求选择模型，再按分析深度选择推理强度。不把较弱模型的高推理档位视为较强模型的固定替代，也不默认让较强模型使用最高推理强度。信息不足以确定实施范围时，先进行能力适当的调查或设计分析。
- 下表为参考路由，不是硬性能力边界或组合白名单。多项匹配时依据任务的关键判断与风险选择；任务范围或关键证据发生实质变化时重新评估，不按失败次数自动升级。

| 任务要求                                                  | `model`         | `reasoning_effort` |
| ----------------------------------------------------- | --------------- | ------------------ |
| 目标明确的检索、提取、分类、结构化摘要、格式转换、文案修正，以及执行指定检查并整理结果；不需要复杂语义判断 | `gpt-5.6-luna`  | `low`              |
| 规则和输入输出明确的简单脚本、模板化局部修改、含少量条件分支的机械处理；风险低且结果容易验证        | `gpt-5.6-luna`  | `medium`           |
| 需求和接口明确的功能实现、边界清晰的多文件修改、单元测试、常规重构、一般缺陷修复和常规代码审查       | `gpt-5.6-terra` | `medium`           |
| 方案和模块边界已确定，但内部逻辑、状态转换、异常路径或测试组合较复杂，需要深入推演的实现、调试与审查    | `gpt-5.6-terra` | `high`             |
| 需要独立工程判断的跨模块实现或重构、接口语义与兼容性分析、技术方案比较和设计评审              | `gpt-5.6-sol`   | `medium`           |
| 复杂根因定位、并发与时序问题、深层性能分析，以及安全边界、鉴权、事务一致性或重大数据迁移的关键实现与审查  | `gpt-5.6-sol`   | `high`             |
| 需要综合多个模块、技术栈和工具证据的复杂端到端实现、集成调试、系统方案设计与专项分析            | `gpt-6-astra`   | `medium`           |
| 高不确定性、长因果链、冲突约束或高风险的系统级实现、重大重构、疑难定位及正确性审查             | `gpt-6-astra`   | `high`             |

- 常规、边界清晰的开发任务以 Terra Medium 为基准，但不把它作为未知任务的试做档位。高于 `high` 的推理档位，仅在委派前已判断需要特别深入的推导、且深度优先于时延和用量时选用。
- 所选组合必须受当前客户端、工具 schema 和目标模型支持，不将界面显示名称直接当作配置值。工具支持时，每次 `spawn_agent` 按当前 schema 显式传入选定的 `model` 和 `reasoning_effort`；配置默认值仅作缺省，不替代逐任务选型。
- 使用自定义代理时，选择未固定模型与推理强度、或固定组合与本次选型一致的角色，避免其配置改变实际选型。
- 目标组合不可用时，在委派前改用能力足够的可用组合，或由具备相应能力的主代理承担；无法满足时说明限制，不静默降级。利用已有调用记录和会话元数据核对实际选型，不额外生成例行选型报告，也不把任务文本中的模型名称或子代理自述当作实际切换的证明。

### 验证与交付

- 子代理返回结果摘要、改动或证据位置、已执行的验证和遗留问题。主代理核查关键证据并完成必要的集成验证，不以子代理声称完成代替整体验收。
- 验证范围由变更影响、验收要求和风险决定。可复用仍能覆盖当前代码、依赖和环境的有效证据，不因更换代理或进入下一阶段而机械重跑全部检查；相关变化使证据失效、存在覆盖缺口或结果矛盾时，补充验证。
- 最终交付说明完成情况、必要的验证结果和未解决事项；未执行的检查不得表述为通过，不把无关的既有问题混入本次实施范围。

## 上下文管理与恢复

- `AGENTS.md` 只保留稳定规则；规格和设计说明预期行为，代码、Git 状态及验证结果说明实际状态，不以聊天记忆或代理摘要替代这些依据。
- 按需读取相关文件和必要上下文，限制搜索、差异与命令输出范围。大段日志留存文件，优先查看相关错误；结论不明时扩大检查，不因截断输出遗漏失败。
- 有 change 时，在阶段完成、重要任务切换、阻塞或结束本轮前更新 `tasks.md` 中的进度、必要验证记录或证据位置、未决问题和下一步；不复制整段聊天记录，不另建重复状态文档。
- 任务状态不清或历史不完整时，先核对相关规格、设计、任务清单、`git status` 和必要的 `git diff`，恢复已完成、待办、约束及阻塞项，再继续修改；仅重做缺失或已失效的工作。

## 实施、验证与交付

- 修复缺陷优先复现并定位原因，补充适用的回归测试；无法复现须说明依据和限制，复杂逻辑优先采用测试先行。失败后先分析原因再重试，重复失败时调整假设或方法；确无可行推进路径时记录阻塞，不机械重复相同失败操作。
- 使用仓库现有测试、静态检查和构建命令，按验收条件、实际影响和风险确定范围；遵守项目已有必需检查，不无故扩大验证，也不遗漏共享模块和集成影响。区分本次回归、既有失败和环境问题。
- 仅复用与当前代码、依赖、配置、环境及验证范围相符的有效证据；相关条件变化后重跑受影响检查。并发改动集成后补足必要验证，不机械重复所有子任务测试。
- 完成判定须有与验收条件对应的可观察证据，如测试、运行结果或可追溯的审查结论；不把“代码已修改”“子代理已完成”或部分测试通过等同于验收通过。不得为通过检查而掩盖失败、删减有效测试或弱化验收条件。
- 任务满足自身验收条件后方可标记完成。声明整个 change 实施完成前，确认其范围内任务和相关规格均已满足；不得将单项任务完成表述为整个 change 完成。关键验证受阻时明确标记未验证，不作完整完成声明。
- 按项目约定及现有授权执行规格同步和归档，确认同步结果；未归档不表述为已归档。交付说明修改摘要、验证命令与结果、遗留问题及变更状态，未执行、失败或受阻的检查须如实标明。
