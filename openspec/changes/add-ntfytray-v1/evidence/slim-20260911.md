# 当前 PNG 图标与精简包验证

用户要求：使用根目录当前 connected.png / disconnected.png，发行包不携带框架或语言资源。

## 实施

- 发布脚本每次执行 New-TrayIcons.ps1，重新从当前两张 PNG 生成对应 ICO；app.ico 与 connected.ico 相同，EXE/通知注册使用 app.ico。源 PNG 不修改。
- 本轮读取的 PNG SHA-256 分别为 E0AF5B75F7C02E344FA1BEA8EB3533F59F63B66D78101C7403D9AF3519DBA979 和 F58EC9D3AA0A3969503C2A1C466FDACABA501AB37E37E0C6BF05D28514A36E8D。与旧候选记录相同；如用户随后再次保存图片，需重新发布。实际提取新 EXE 图标并观察为绿色勾图案。
- SelfContained=false、WindowsAppSDKSelfContained=false；保留非单文件/非裁剪，不打包语言卫星资源、PDB、自包含框架。
- 总 SDK 依赖改为 Foundation 2.3.9、InteractiveExperiences 2.1.6、Runtime 2.4.0，传递 Base 2.0.4。InteractiveExperiences 是 Foundation 所需依赖，版本满足 Runtime 2.4 组件校验；未手工删除必需库。
- 不再分发 AI、ML、WinUI、Search、Widgets、WebView2。剩余 Microsoft.Windows.SDK.NET.dll 是应用调用 Windows API 的投影依赖，不是可随意删除的运行库安装包。
- README/OpenSpec 更新为依赖系统安装 .NET 10 Desktop Runtime x64 和 Windows App Runtime 2.4 x64。

## 候选与检查

目录：artifacts/candidates/20260911-225351。
ZIP：NtfyTray-win-x64.zip；SHA-256：62E7E835BBF11EFD62B31468D0410FAC330DB911D0C99B6C2A44D76D67ED3C3C。

旧 ZIP 79,586,348 bytes；新 ZIP 8,241,369 bytes（7.86 MiB），减少约 89.64%。新包 34 文件、29,428,563 bytes（28.07 MiB）。详细源码、依赖锁、文件和 ZIP 指纹见 candidate-manifest.json。

通过工具脚本执行锁定还原、Release 构建、184/184 测试、依赖框架发布、ZIP 解压指纹一致性、凭据排除和无框架/语言/PDB检查。构建 0 错误，保留 2 条 NU1900 警告：无法获取 NuGet 漏洞信息，漏洞审计未完成；未关闭或隐藏该检查。

普通用户从 TEMP 工作目录运行候选 expanded/NtfyTray.exe --smoke-test，2026-09-11 22:54:29 托盘启动，22:54:30.971 MessageSubmitted，随后正常退出，exit=0。无真实配置的候选记录预期 ConfigurationRejected，未发起订阅。这是原生 API 提交证据，不等于用户确认横幅。

另在 artifacts/local-test/20260911-225351 单独复制包及真实 config.yaml，保持 ZIP 不含配置。旧版由用户退出，新版 PID 18024 已启动用于用户图标/连接验收，运行文件与候选一致。没有发布外部消息或修改远端服务。

## 证据范围

11.1–11.3 完成。未改动订阅、通知业务实现，但发布依赖变化；旧候选的完整实机/长期证据不能直接当作新包通过。8.2–8.5、9.4 仍待验收。未同步规格、未归档。
