# 候选交付记录：2026-09-11

## 候选与验证

候选编号：20260911-224101。目录：`artifacts/candidates/20260911-224101/`。

- `candidate-manifest.json` 集中保存源码和锁文件 SHA-256、命令、运行文件、解压文件及测试结果。
- `NtfyTray-win-x64.zip` SHA-256：`937CADF9D7E49074BF0D9717D93B87BE8787A156011E741D7AAAADB0A6F27AAD`。
- `tools/Publish-Candidate.ps1` 执行锁定还原、Release 构建、完整测试、自包含发布、压缩和解压核对；详细命令和输出见候选清单及 verification 目录。184/184 测试通过，构建零警告、零错误。
- 候选不含真实 config.yaml，凭据排除检查通过。交付前再次核对源码指纹，无漂移。原始 PNG 未改动。
- 发布为 win-x64、自包含 .NET、非单文件、非裁剪；Windows App Runtime 说明见包内 README。

## 完成任务依据

- 1.1–1.4：见 implementation-2-20260911.md 的 T0 普通用户注册、显示、热激活、真正退出后冷激活、无窗口单实例及文件夹发布记录。用户明确确认按顺序退出再点击后托盘恢复。
- 2.1–2.3：正式 ApplicationContext、菜单、STA 分发、用户/会话 Mutex、可取消幂等停止；生命周期、分发与状态测试通过。图标已检查原显示及代表性小尺寸缩放预览，未切换整个系统 DPI。
- 3.1–3.4：70 项配置测试和 7 项日志测试；错误配置不创建订阅管理器，TEMP 工作目录启动实测正确定位配置；敏感字段不进入日志，故障按来源保持。
- 4.1–5.5：协议、退避、恢复及 22 项订阅测试覆盖请求编码、可选认证、分块、多事件、响应头与空闲预算、取消、HTTP 分类、恢复信号、去重游标与有界回退。非法 TLS 不绕过平台验证，默认禁用重定向。罕见分支使用夹具，不能视为实服全部通过。
- 游标拒绝夹具依据 ntfy 官方 server/errors.go 的 HTTP 400 / code 40008；消息与截断契约依据 docs.ntfy.sh/subscribe/api/。目标服务版本为用户报告的 2.28.0，未独立读取管理员版本端点。
- 6.1–6.2：独立 Topic 循环、共享 HttpClient、顺序处理和集中取消释放；隔离、单活动连接、恢复与退出测试通过。
- 7.1–7.3：Unicode 格式化、设置抑制、最多三次尝试、三种结果与故障恢复测试通过；正式通知服务统一手动测试与业务提交，原生激活证据见 T0 记录。
- 8.1：完整 184 项自动化通过；平台及真实链路验收范围保留在 8.2–8.5，未用单元测试替代。
- 9.1–9.2：候选清单、ZIP、示例配置与 README 已生成并验证。
- 9.3：按用户明确指示取消代理干净 Windows 测试，交付候选和运行说明，最终部署验收交由用户。本项是交接完成，不是用户已验收通过。

## 独立目录真实只读连接

从候选解压内容复制至 `artifacts/local-test/20260911-224101/`，另外加入本地真实配置；原候选目录和 ZIP 保持不变。该配置不应分享。没有发布外部消息，也没有修改 ntfy、Tunnel 或系统网络。

普通用户环境从 TEMP 工作目录启动 NtfyTray.exe，PID 1084。2026-09-11 22:44:41 配置接受、托盘启动；22:44:42.691 +08:00 记录 `TopicConnected`，Topic 为 codex。只存在一个 NtfyTray 进程，MainWindowHandle=0。日志路径为 `%LOCALAPPDATA%/NtfyTray/logs/ntfytray-20260911.log`，不含 Token 或消息正文。程序保持运行供用户验收。

此证据仅证明配置加载、认证订阅及有效 SSE 事件连接，不证明全部真实消息/故障场景或横幅实际展示。

## 待验收与状态

8.2 真实消息与故障恢复完整场景、8.3 系统交互、8.4 有限样本 P95、8.5 首次 24 小时未完成；9.4 正式发行未完成。未启动无人值守监测、未改变验收阈值，未声明整个 V1 已验收完成。未同步或归档 change。
