## 1. T0 通知与运行环境验证

- [x] 1.1 创建一个主项目和 xUnit 测试项目，按 design 设置 WinExe、目标框架、win-x64、asInvoker 和稳定依赖锁定；验收：还原、构建成功，项目属性和锁文件可核查。
- [x] 1.2 实现最小无窗口托盘和原生本地测试通知，验证未打包 SDK 初始化、注册名称图标与通知设置读取；验收：普通用户目标 Win10 显示一个托盘图标和一条系统通知，无业务窗口和控制台。
- [x] 1.3 验证运行中及退出后点击通知的 SDK 激活路径，记录主实例协作机制；验收：点击不执行消息业务动作、不创建重复接收实例。
- [x] 1.4 生成最小 .NET 自包含文件夹发布，在目标 Win10 22H2 x64 验证脱离源码目录启动、通知及运行库检查方法；验收：记录系统、稳定 SDK/运行库版本、安装检查命令及结果。T0 保持优先，最终部署交接见 9.3，不要求代理执行干净机测试。

## 2. T1 程序骨架与生命周期（2.1、2.3 依赖 1.2；2.2 另依赖 1.3）

- [x] 2.1 完成 ApplicationContext、固定菜单、双状态图标及 UiDispatcher，从根目录 connected.png/disconnected.png 生成并打包对应 ICO，保留原图；验收：仅有就绪 connected 与故障/未就绪 disconnected 两种图案，过渡及故障文字正确，在当前显示设置及一个代表性缩放比例下小尺寸可辨识，切换释放资源，所有控件访问在主线程；不穷举 DPI 与主题组合。
- [x] 2.2 完成用户及会话范围单实例保护并整合 T0 激活处理；验收：重复普通启动直接结束、无第二图标，通知激活测试仍通过。
- [x] 2.3 建立统一退出令牌、后台任务观察及幂等异步停止流程；验收：假后台任务可取消并释放，退出不使用主线程同步等待或强制杀进程。

## 3. T2 配置与基础日志（依赖 T1）

- [x] 3.1 实现配置模型、默认值、BaseDirectory 路径及严格 YAML 校验；验收：正常/缺失配置、错误缩进、未知键、重复键、类型和数值边界、版本、URL、Topic 数量/重复/空值测试通过。
- [x] 3.2 实现配置失败托盘状态和全量校验后启动，生成 config.example.yaml 并忽略真实配置；验收：无效配置产生零网络请求，换工作目录仍定位正确，修改后仅重启生效。
- [x] 3.3 实现启动前基础日志及 configuration-logging 规定的默认值和可配置滚动保留；验收：默认值与定制值映射正确，用临时目录和较小阈值做一次滚动及保留接入测试，跨日配置可核查，日志默认目录正确、字段可诊断；不等待真实跨日或全面复测 Serilog 内部行为。
- [x] 3.4 实现脱敏及日志写入失败的独立状态反馈；验收：Token、标题、正文和敏感异常响应不进入默认日志，磁盘失败不递归写日志，故障在本进程保持并提示重启；重启后基础日志初始化及首次写入成功才视为恢复，其他组件或偶然写入成功不清除此故障。

## 4. T3 单 Topic SSE（依赖 T2）

- [x] 4.1 实现共享 HttpClient、单 Topic 请求、可选 Bearer、响应媒体类型检查和禁止自动重定向；验收：请求路径/头正确，HTML、重定向、非法证书不被当作正常 SSE，凭据不转发。
- [x] 4.2 实现 SseParser 和 ntfy JSON 分流校验；验收：open、keepalive、message、未知事件、缺失/额外字段、坏 JSON、事件跨读取、多事件合并及中文跨字节分块测试通过。
- [x] 4.3 分离响应头预算与事件空闲监控、管理流释放及取消；验收：取得响应头后不被 15 秒阶段计时误杀、200 尚无有效事件不标已连接、连接/读取都可取消。

## 5. T4 重连与会话恢复（依赖 T3）

- [x] 5.1 实现可注入时钟、延迟和随机源的退避策略；验收：指数增长、基准上限、扰动范围、60 秒稳定重置和抖动连接不重置测试通过，不真实等待数分钟。
- [x] 5.2 实现 HTTP/传输错误分类；验收：DNS/EOF/超时/5xx 重试、429 Retry-After 秒数/日期及无效值、401/403 300 秒低频、404/代理错误待重启、TLS 不绕过、主动取消不报故障均有测试。
- [x] 5.3 实现有效事件心跳监控与恢复信号合并；验收：仅心跳连接不重连、无效 JSON 不保活、假死重连，连续睡眠/网络信号先停旧连接且最大活动连接数为一；信号不缩短或重新起算普通退避/429/401/403 等待，睡眠已过截止时间可重试，404/代理配置故障不被信号重新激活，退出优先且取消全部等待。
- [x] 5.4 实现每 Topic 2048 ID 有界去重和处理完成后更新游标；验收：重复 ID、同文异 ID、容量淘汰、Submitted/Suppressed/Dropped、处理中断不推进、重复旧消息不倒退游标测试通过。
- [x] 5.5 实现消息 ID/首次连接服务器时间恢复及游标拒绝的有界回退；验收：首次无 since、同进程恢复、无业务 ID、无时间参考、明确游标拒绝及普通 400 不误分类测试通过；成功 SSE 响应带 X-Messages-Truncated: 1 时只记录缺失提示，继续消费缓存和实时消息、不重置 since、不触发回退；记录目标 ntfy 版本及脱敏响应夹具，open/keepalive ID 从不作为业务游标。

## 6. T5 多 Topic 管理（依赖 T4）

- [x] 6.1 实现 SubscriptionManager 对 1–20 个独立 Topic 的启动、停止和状态汇总；验收：单 Topic 403 不影响其他订阅，全部就绪显示 connected，部分/全断/组件故障显示 disconnected，启动和退出不增加第三种图标；来源恢复只清除自身故障，连接恢复不掩盖日志故障，测试通知成功不清除 Topic 权限故障，全部故障解除才恢复 connected，每 Topic 最多一个活动任务。
- [x] 6.2 整合跨 Topic 调度、逐 Topic 顺序处理和生命周期资源释放；验收：无无界消息队列、无逐消息 Task.Run，重复恢复与退出不泄漏任务、流和事件订阅。

## 7. T6 通知转换与失败处理（依赖 T1；完整配置集成依赖 T2）

- [x] 7.1 实现 NotificationFormatter 和通知服务契约；验收：空白标题回退、纯文本、中文/换行/XML 特殊字符、Unicode 安全截断、128/1024 上限含来源及省略号、include_topic 开关测试通过。
- [x] 7.2 实现配置关闭和系统设置抑制、提交结果以及最多总计三次的可取消尝试；验收：三种结果与取消测试通过，持续失败不无限阻塞后续消息；初始化/消息处理/手动测试触发设置复查，允许后只清除设置故障，Submitted 只清除提交故障，Suppressed 不冒充恢复，初始化故障重启成功后才清除，不新增轮询或后台初始化循环。
- [x] 7.3 将本地测试通知、业务提交与激活整合到同一服务；验收：click/actions/attachment 不引发命令、下载或额外请求，普通退出仅清理运行资源，原生通知及单实例实机回归通过。

## 8. T7 自动化与集成验收（8.1 依赖 T5、T6；8.2–8.5 依赖候选包 9.1）

- [x] 8.1 运行完整自动化测试并核对五项能力场景均有适当验证依据或后续实机安排；验收：配置、消息、分块、格式、去重、重连、取消、隔离和脱敏自动化均通过，记录命令及结果；参数化用例可覆盖同类边界，不要求每个场景同时具备单元、集成和实机测试，不重复补建已有有效覆盖。
- [ ] 8.2 使用已完成 9.1 的候选包在授权测试环境验证真实 ntfy 和固定域名 Tunnel；验收：正常消息、浏览器完全退出、认证成功及 Token 错误、断网、ntfy/Tunnel 重启、有效缓存补收、进程退出期间不补收及实时流无缓冲均通过。难以安全、稳定触发的游标拒绝、有效截断和 HTML 异常响应，可引用 4.1、5.5 的可信夹具测试，记录夹具来源、目标服务版本及实服未验证范围；游标拒绝与截断分别验证，不把缺少提示当作缓存完整。上述真实主链未验证时本项不得完成；罕见分支按发布规格完成夹具验证并说明限制后不单独阻塞本项。
- [ ] 8.3 在目标 Win10 用候选包连接 8.2 所用的真实 ntfy 与固定域名 Tunnel 链路，验证系统睡眠恢复、关闭及重新允许应用通知、横幅/专注设置下的提交与展示区别，并在事件读取和重连等待中退出；验收：无重复订阅，故障按来源清除，提交不误报阅读，正常退出三秒内且无残留。其他取消阶段及恢复信号与各类等待的组合复用 2.3、5.2、5.3、7.2 自动化证据，不逐一实机重演；发现平台特有异常时补测受影响场景。
- [ ] 8.4 在一次正常负载联调中，用一台目标 Win10 和候选包采集一组有限样本，采样前确定发送速率、样本数及其选择依据和 P95 算法；验收：完整消息取得至通知 API 调用的 P95 不超过一秒，排除互联网和 Windows 展示时间，不以单条消息代替统计。可与 8.2 或 8.5 合并采样，无需独立性能平台、多机或重复多轮压测。
- [ ] 8.5 首次正式交付前完成一次连续 24 小时实机稳定性验收；验收：运行内容与候选清单一致，无异常退出，记录内存/线程/句柄趋势且无持续异常增长。首次未执行或失败保持未完成；已有通过记录后按 windows-distribution 的影响规则决定完整重跑或复用，不因任意运行文件变化一律重新计时。

## 9. T8 候选包准备与交付（9.2 → 9.1；9.3 为用户部署验收交接；9.4 在 T7 全部通过后）

- [x] 9.1 在 8.1 和 9.2 完成后整理锁定依赖的还原/构建/测试/发布命令，生成候选 ZIP；验收：稳定依赖锁定、依赖系统运行库的精简发布、非单文件/非裁剪，包含由两张指定 PNG 派生的托盘资源及示例/说明，脱离源码目录仍可显示两种图标，无真实配置或凭据；在一份候选清单集中记录源码基线、锁文件、命令、ZIP SHA-256、解压文件清单与运行文件指纹，后续验收引用该清单。
- [x] 9.2 在 T0、T5、T6 完成后编写 README 的配置、重启生效、日志、只读 Token、运行库安装检查、通知验证、错误排查、更新及回退说明，供 9.1 打包；验收：与规格和 T0 实际版本一致，写明日志及通知初始化故障需修复后重启、通知设置及提交故障的复查恢复方式，说明底层运行库缺失可能早于托盘初始化失败。
- [x] 9.3 在 9.1 完成后交付候选包、清单和运行说明，由用户进行最终部署验收；验收：按用户 2026-09-11 指示，代理不执行无开发工具的干净 Windows 测试，明确交接和已验证范围，不把交接完成或开发机可运行写成用户已验收通过；本项不阻塞本轮代码交付及 8.2–8.5。
- [ ] 9.4 在 T7 全部通过后核对所有规格、任务与交付证据，直接将已验收候选 ZIP 作为发行包并记录 change 状态；验收：源码、锁文件、示例、ZIP、运行库说明齐全，各任务引用候选清单及适用验证记录才声明 V1 完成，不重新构建；如候选内容变化，按 windows-distribution 记录差异、证据复用理由及受影响验收结果后再判定，规格同步/归档仅在对应授权流程执行。

## 10. 调度、验证记录与当前状态

2026-09-12 退出诊断：用户报告不到 24 小时托盘消失，现场确认无进程；最新会话 09-11 22:55 启动，09-12 10:14:20 最后成功重连，此后无正常停止记录。用户确认关闭/重启过 Codex；历史启动来源和当前 Windows Job 的 KillOnJobClose 标记支持“随 Codex 管理的 Job 终止”为最可能原因，但缺少旧 PID 退出审计，不能完全证实历史因果及精确时刻。8.5、9.4 保持未完成。详见 [退出诊断记录](evidence/unexpected-exit-20260912.md)。本轮仅分析并记录证据，未修改应用、未重启、未复现或修复；下一步由用户直接启动同一候选，核对与 Codex 生命周期独立并重新验收。

当前进度：31/36。图标与精简发布已完成，最新候选 20260911-225351，详见 evidence/slim-20260911.md。核心实施、184 项自动化测试、候选打包与用户验收交接完成；8.2–8.5 和 9.4 保持未完成。详见 [候选交付记录](evidence/candidate-20260911.md)。

依赖链：1.1 → 1.2 → 1.3、1.4；1.2 → 2.1、2.3；1.3 + 2.1 → 2.2；T1 → T2 → T3 → T4 → T5；T1 → T6（配置集成等待 T2）；T5 + T6 → 8.1；T0 + T5 + T6 → 9.2；8.1 + 9.2 → 9.1 → 8.2–8.5；9.1 → 9.3（用户验收交接）；T7 全部通过 → 9.4。任务编号保持不变，9.2 先于 9.1 执行。

T0 通知与最小发布优先推进；环境准备或通知验证等待期间，在 1.1 完成并固定契约后可提前实现配置校验、事件解析、退避和格式化等纯逻辑部分，所属集成任务仍须满足上面的依赖才能标记完成。T2 与 T6 可按文件边界并行；Program、TrayApplicationContext、项目依赖和通知契约由主代理协调，先固定接口再并行。通知路线未验证不得声明集成或候选发布完成，最终部署验收交由用户，不要求代理准备干净环境。全局进度仅更新本文件。

实施验证命令基线（工程创建后执行）：`dotnet restore --locked-mode`、`dotnet build -c Release --no-restore`、`dotnet test -c Release --no-build`，发布主项目使用 `dotnet publish src/NtfyTray/NtfyTray.csproj -c Release -r win-x64 --self-contained false` 并核对项目关闭单文件与裁剪。首次生成锁文件的还原与后续锁定还原应区分。若命令受环境限制，记录失败原因，不标通过。

每项任务完成时在本文件补充日期、简短结果及证据相对路径；命令和详细结果集中保存一次，多个任务可引用同一有效记录。较长记录可放本 change 的 evidence 目录，不放凭据、完整消息正文或重复计划。核心分支用参数化自动测试，第三方库用必要的配置核查和接入测试；已通过且相关代码、配置与环境未变时复用证据，不追求覆盖率数字、穷举组合或为每项功能重复建立多层测试。

实机证据引用 9.1 的候选清单及集中记录的配置、环境，9.3 记录用户交接；更换机器或配置时只补充差异并核对适用范围。源码尚无 Git 基线时记录文件指纹。共享系统资源的操作合理串行；8.4 可合并采样，但会退出或重启客户端的测试安排在 24 小时计时之外，计时中不更换运行内容。候选变化后的重测以 windows-distribution 为准：首次通过记录必须存在，影响连接循环、并发、资源生命周期或相关依赖/发布配置时重跑 24 小时；无此影响且有依据时复用长期记录并完成受影响检查，不能用复用掩盖失败或声称新包实际跑满 24 小时。

历史候选状态（2026-09-11 22:45）：候选 20260911-224101 已完成锁定还原、Release 构建、184/184 测试、自包含发布、ZIP 解压指纹核对和真实配置排除检查。独立运行目录从 TEMP 工作目录启动，配置成功，codex Topic 已连接，PID 1084，无业务窗口。服务器版本 2.28.0 来自用户部署侧报告；不申请管理员凭据。按用户要求不执行无开发工具的 Windows 测试，最终部署验收由用户完成。真实链路全场景、P95、首次 24 小时及正式发行仍待验收，不把握手成功等同于 8.2 完成。目录非 Git 仓库，以候选清单源码指纹为基线；未同步现行规格、未归档。

历史修订（2026-09-11，前轮）：区分游标失效与缓存截断，补齐恢复信号优先级及按来源清除故障，改为候选包先于实机验收，集中默认值和依赖；备份及基线位于 `.backups/openspec-review-20260911-181513/`。其中全场景实服、T0 干净机前置及任意运行变更重跑 24 小时的旧验收规则已由本轮修订替代。

历史校验（2026-09-11，前轮）：严格校验、链接、代码围栏、33 个任务编号和依赖无环检查通过；旧差异与检查结果位于上述备份目录的 `review.diff` 和 `verification.json`，不作为本轮修订的验证结果，也不代表实施完成。

历史修订（2026-09-11，验证范围调整）：按用户确认的评估建议调整验证分层、T0 调度、实服与夹具分工、有限样本性能采样、长期复测范围及集中证据；保留全部功能、安全边界、一秒 P95、三秒退出、首次 24 小时和最终候选干净环境验收。更新 proposal、design、tasks、windows-distribution 及来源方案，其余四项能力规格未改。修改前五份文档和基线位于 `.backups/openspec-validation-scope-20260911-183628/`；实施仍为 0/33。

历史复审（2026-09-11，实施前）：主代理与独立子代理核对规格、设计、任务依赖及来源方案，已修复睡眠恢复真实链路范围、P95 旧措辞及 HTML 夹具边界，并明确采样前确定样本数依据；复核未发现遗留冲突。`openspec validate add-ntfytray-v1 --strict` 通过，33 个唯一未勾选任务、17 个本地链接及代码围栏检查通过；四项功能规格、规则、图标和环境文档保持基线。差异及检查摘要保存于本轮备份目录的 `review.diff` 和 `verification.json`。未执行应用构建、自动化测试或实机验收，未同步现行规格、未归档；下一步按修订任务从 T0 开始实施。






## 11. 当前图标与精简发布（用户明确要求）

- [x] 11.1 从当前根目录 PNG 重新生成双状态 ICO，EXE 与通知注册图标统一采用 connected.png。
- [x] 11.2 改为依赖运行库发布，移除未使用 SDK 组件及语言资源，更新运行说明和发布检查。
- [x] 11.3 构建、测试、生成新候选并验证启动/本地通知，记录大小与清单；既有实机验收状态不冒充新包通过。


